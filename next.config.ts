import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['dummyimage.com', 'localhost', '127.0.0.1', 'placehold.co', 'giveapaw.ivelse.com', 'gapow.ivelse.com', 'gapow.com', 'gapow.com'],
  },
};

export default nextConfig;
